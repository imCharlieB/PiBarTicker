"""Logo and team metadata caching system.

This module handles downloading, storing, and serving team logos locally
instead of relying on remote ESPN URLs or the old per-league teamStyles blob in config.json.
"""